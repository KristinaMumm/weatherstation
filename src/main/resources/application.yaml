spring:
  application:
    name: weatherstation

  datasource:
    url: jdbc:mysql://localhost:3306/weatherstation?allowPublicKeyRetrieval=true&useSSL=false
    username: user
    password: password

  flyway:
    enabled: true
    locations: classpath:db/migration
